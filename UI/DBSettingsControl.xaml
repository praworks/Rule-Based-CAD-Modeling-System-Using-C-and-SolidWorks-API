<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="SolidWorks.TaskpaneCalculator.UI.DBSettingsControl"
             Width="400" Height="300">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">Database Type:</TextBlock>
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="DbTypeCombo" SelectedIndex="0">
            <ComboBoxItem>MongoDB</ComboBoxItem>
            <ComboBoxItem>SQLite</ComboBoxItem>
            <ComboBoxItem>File</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,8,0,0">Connection URI:</TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtUri" Margin="0,8,0,0" />

        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,8,0,0">Database Name:</TextBlock>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtDb" Margin="0,8,0,0" />

        <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,8,0,0">Password (dev - plaintext):</TextBlock>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtPassword" Margin="0,8,0,0" />

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnSave" Width="80" Margin="4" Content="Save" />
            <Button x:Name="BtnLoadEnv" Width="120" Margin="4" Content="Load from Env" />
        </StackPanel>
    </Grid>
</UserControl>
