<UserControl x:Class="AICAD.UI.TextToCADTaskpaneWpf"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:AICAD.UI"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="472" d:DesignHeight="1200"
             Focusable="True">

    <UserControl.Resources>
        <ui:NullToCollapsedConverter x:Key="NullToCollapsedConverter" />
        <ui:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="6,2,6,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="35"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#FFEEEEEE" BorderThickness="0,0,0,1" Margin="0,0,0,3">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Top">
                <Button Content="New Project" Padding="8,2" Margin="0,0,5,3" Background="Transparent"/>
                <Button Content="Import" Padding="8,2" Margin="0,0,5,3" Background="Transparent"/>
                <Button x:Name="addSeriesButton" Content="+ Series" Padding="8,2" Margin="0,0,5,3" Background="Transparent" Foreground="#005A9E"/>
            </WrapPanel>
        </Border>

        <StackPanel Grid.Row="1" Margin="0,0,0,4">

            <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,2" Padding="4,2">
                <StackPanel x:Name="KaraokeLinesPanel">
                    <Label Content="Select Series" Foreground="#005A9E" FontWeight="SemiBold" Padding="0"/>
                    <DockPanel>
                        <Label Content="Series:" FontSize="10" Foreground="Gray" Padding="0"/>
                        <Label x:Name="nextSequenceLabel" Content="Next Sequence: 0018" FontStyle="Italic" Foreground="Gray" FontSize="10" HorizontalAlignment="Right"/>
                    </DockPanel>
                    <ComboBox x:Name="seriesComboBox" Margin="0,1,0,2" SelectedIndex="0">
                        <ComboBoxItem>ASM</ComboBoxItem>
                        <ComboBoxItem>PRT</ComboBoxItem>
                        <ComboBoxItem>DRW</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Border>

            <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,2" Padding="4,2">
                <StackPanel>
                    <Label Content="Material" Foreground="#005A9E" FontWeight="SemiBold" Padding="0"/>
                    <Label Content="Select Material:" FontSize="10" Foreground="Gray" Padding="0"/>
                    <ComboBox x:Name="materialComboBox" Margin="0,1,0,0" SelectedIndex="0" IsEditable="True">
                        <ComboBoxItem>Steel, Mild</ComboBoxItem>
                        <ComboBoxItem>Aluminum 1060 Alloy</ComboBoxItem>
                        <ComboBoxItem>Stainless Steel</ComboBoxItem>
                        <ComboBoxItem>Brass</ComboBoxItem>
                        <ComboBoxItem>Copper</ComboBoxItem>
                        <ComboBoxItem>Bronze</ComboBoxItem>
                        <ComboBoxItem>Titanium</ComboBoxItem>
                        <ComboBoxItem>ABS PC</ComboBoxItem>
                        <ComboBoxItem>Polycarbonate</ComboBoxItem>
                        <ComboBoxItem>Polypropylene</ComboBoxItem>
                        <ComboBoxItem>PVC Rigid</ComboBoxItem>
                        <ComboBoxItem>Nylon 6/10</ComboBoxItem>
                        <ComboBoxItem>Cast Alloy Steel</ComboBoxItem>
                        <ComboBoxItem>Plain Carbon Steel</ComboBoxItem>
                        <ComboBoxItem>Alloy Steel</ComboBoxItem>
                        <ComboBoxItem>Aluminum 6061</ComboBoxItem>
                        <ComboBoxItem>Aluminum 2014</ComboBoxItem>
                        <ComboBoxItem>Aluminum 5052</ComboBoxItem>
                        <ComboBoxItem>Aluminum 7075</ComboBoxItem>
                        <ComboBoxItem>304 Stainless</ComboBoxItem>
                        <ComboBoxItem>316 Stainless</ComboBoxItem>
                        <ComboBoxItem>Custom</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Border>

            <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,2" Padding="4,2">
                <StackPanel>
                    <Label Content="Type Description" Foreground="#005A9E" FontWeight="SemiBold" Padding="0"/>
                    <Label Content="Description:" FontSize="10" Foreground="Gray" Padding="0"/>
                    <TextBox x:Name="typeDescriptionTextBox" Margin="0,1,0,0" Height="22" IsReadOnly="False" IsEnabled="True"/>
                </StackPanel>
            </Border>

            <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,2" Padding="4,2">
                <StackPanel>
                    <Label Content="Weight" Foreground="#005A9E" FontWeight="SemiBold" Padding="0"/>
                    <Label Content="Weight (kg):" FontSize="10" Foreground="Gray" Padding="0"/>
                    <TextBox x:Name="weightTextBox" Margin="0,1,0,0" Height="22" Background="#FAFAFA" IsReadOnly="True"/>
                </StackPanel>
            </Border>

            <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,2" Padding="4,2">
                <StackPanel>
                    <Label Content="Generated Name Preview" Foreground="#005A9E" FontWeight="SemiBold" Padding="0"/>
                    <TextBox x:Name="previewTextBox" Text="ASM-0018" FontWeight="Bold" HorizontalContentAlignment="Center" IsReadOnly="True" Margin="0,2,0,0" Height="20" VerticalContentAlignment="Center" BorderBrush="#005A9E"/>
                </StackPanel>
            </Border>

            <StackPanel Margin="0,3,0,0">
                <Button x:Name="saveWithNameButton" 
                        Content="Save With This Name" 
                        Height="22" Margin="0,0,0,2" 
                        Background="#0066CC" Foreground="White" FontWeight="Bold" BorderThickness="0"/>

                <Button x:Name="applyPropertiesButton" 
                        Content="Apply Properties" 
                        Height="22" Margin="0,0,0,2" 
                        Background="#F0F0F0" Foreground="#003366" BorderBrush="#CCCCCC" BorderThickness="1"/>
            </StackPanel>

        </StackPanel>

        <Grid Grid.Row="3" Margin="0,2,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="1" Background="Transparent" CornerRadius="6" Padding="12,4" Margin="0,0,0,4">
                <StackPanel x:Name="ProgressStatusPanel">
                    <ItemsControl x:Name="StepsItemsControl" Margin="0,0,0,2">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="28"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="14" Text="●" Foreground="Gray" Visibility="{Binding State, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Pending}"/>
                                    <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="14" Text="⟳" Foreground="#FFC107" Visibility="{Binding State, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Running}"/>
                                    <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="14" Text="✔" Foreground="#28A745" Visibility="{Binding State, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Success}"/>
                                    <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="14" Text="⚠" Foreground="#DC3545" Visibility="{Binding State, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Error}"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Label}" FontWeight="SemiBold" FontSize="11"/>
                                        <TextBlock Text="{Binding Message}" FontSize="10" Foreground="Gray"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <ProgressBar Width="50" Height="8" Value="{Binding Percent}" Maximum="100" Visibility="{Binding Percent, Converter={StaticResource NullToCollapsedConverter}}"/>
                                        <TextBlock Text="{Binding Percent, StringFormat='{}{0}%'}" Margin="4,0,0,0" FontSize="10" VerticalAlignment="Center" Foreground="#007ACC" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock x:Name="KaraokeStatus" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,2,0,4" Foreground="#333333"/>
                </StackPanel>
            </Border>

            <Grid Grid.Row="2" Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ProgressBar x:Name="generationProgressBar" Grid.Column="0" Value="0" Height="6" Foreground="#007ACC" Background="#E0E0E0" BorderThickness="0"/>
                <TextBlock x:Name="generationProgressText" Grid.Column="1" Text="0%" Foreground="#007ACC" FontSize="10" Margin="6,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
            </Grid>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="90"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="prompt" Grid.Column="0" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="6" Text="Enter prompt..." GotFocus="prompt_GotFocus" LostFocus="prompt_LostFocus" PreviewKeyDown="prompt_PreviewKeyDown" BorderBrush="#FFCCCCCC" Height="100" VerticalAlignment="Center"/>
                <Button x:Name="build" Grid.Column="1" Content="Build 🚀" Margin="4,0,0,0" Background="#FF007ACC" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Height="100" VerticalAlignment="Center"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label x:Name="presetLabel" Grid.Column="0" Content="Preset:" VerticalAlignment="Center" Foreground="DimGray" FontSize="11"/>
            <ComboBox x:Name="shapePreset" Grid.Column="1" VerticalAlignment="Center" SelectedIndex="0" Margin="4,0" Height="24">
                <ComboBoxItem>— none —</ComboBoxItem>
                <ComboBoxItem>Box 100x50x25 mm</ComboBoxItem>
                <ComboBoxItem>Cylinder Ø40 x 80 mm</ComboBoxItem>
            </ComboBox>
            <Label x:Name="lblVersion" Grid.Column="2" Foreground="Silver" Content="v1.0" VerticalAlignment="Center" FontSize="9"/>
        </Grid>

        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button x:Name="btnThumbDown" Content="👎" Background="Transparent" BorderThickness="0" Margin="0,0,3,0"/>
                <Button x:Name="btnThumbUp" Content="👍" Background="Transparent" BorderThickness="0"/>
                <Label x:Name="feedbackLabel" Content="Successful?" Foreground="DimGray" VerticalAlignment="Center" Margin="3,0,0,0" FontSize="11"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="btnHistory" Content="History" Padding="6,1" Margin="3,0,0,0" FontSize="11"/>
                <Button x:Name="btnStatus" Content="👁️" Padding="6,1" Margin="3,0,0,0"/>
                <Button x:Name="btnSettings" Content="⚙️" ToolTip="Settings" Padding="4,1" Margin="3,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>